# WeTube 환경 변수 예시
# 이 파일을 복사해 .env 로 저장한 뒤 실제 값을 채워 넣으세요.
# .env 파일은 .gitignore에 포함되어 Git에 커밋되지 않습니다.

# ----- Cloudinary (동영상·썸네일 업로드) -----
# Cloudinary 대시보드(https://console.cloudinary.com)에서 확인
# 설정되지 않으면 로컬 uploads 폴더에 저장됨
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
# 프록시 경유 시 (PA 등): http://proxy.server:3128
# CLOUDINARY_API_PROXY=

# ----- DB (선택) -----
# DATABASE_URL=sqlite:///instance/wetube.db
